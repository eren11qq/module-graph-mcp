# COMPROMISES — 已知妥协清单

每一处"先这样"都有记录：它是什么、代价是什么、以及**何时必须回头修**。
回头修的触发条件写死，避免妥协悄悄烂在代码里。

| # | 妥协 | 代价 | 何时回头修 |
|---|------|------|-----------|
| 1 | 仅解析相对导入（`./x`、`../x`）；裸导入（npm 包）与跨包别名不算边 | 图不含第三方依赖——这正是设计意图，但 monorepo 内部包别名会被当成外部而漏边 | 出现跨包/裸导入的误判影响真实项目时 |
| 2 | TS7 scanner 按 token 词法解析（非完整 AST），靠 `assertScannerContract` 响亮失败兜底 | scanner 对新语法的容错弱于完整解析器；契约测试先红再修 | TS 升级打破 scanner 契约时（契约测试会先红） |
| 3 | coveredBy 靠命名约定：源文件扩展名（ts/tsx/js/jsx）+ 测试文件判定 = 文件名以 `.test`/`.spec` 结尾，或位于任意 `__tests__/` 段下（`coverage.ts isTestFile`） | 命名约定是唯一覆盖来源：命名不走约定的测试文件（如 `x_test.ts`、`tests/x.ts`）既不算 coveredBy 也不算测试文件 | 命名约定误报出现时（README/MODULE-DESIGN 同口径） |
| 4 | coverage 判定依赖 istanbul `json-summary` 文件**存在即通过**，不校验阈值 | 没有覆盖率门槛语义——文件在报告里出现才算 covered，0% 与 100% 同色 | 需要覆盖率阈值时 |
| 5 | 内容型 MCP 工具有界等待基线扫描（20s 上限），超时后对**部分图**应答 | 扫描极慢的仓库里，超时后的读可能拿到不完整节点表（响应带 scanning 注记） | 基线扫描慢到 20s 不够时 |
| 6 | 仅支持 ts/tsx/js/jsx 四种语言 | 其他语言模块（css、py、go…）不进图 | 出现其他语言模块的诉求时 |
| 7 | 同根去重靠端口带扫描（首选端口起 20 个连续端口） | 端口带被其他服务挤占时，同根副实例可能认不出主实例而各自弹窗 | 端口带 20 不够用或探测不稳时 |
| 8 | 风险级启发式（GitNexus 移植）是粗粒度规则：波及在环上或 top-20% 高中心度 → high、受影响 >10 → medium；token 估算按 4 字节/token 整体上界，不做真分词 | 风险分级会漏判细粒度风险（如「改了 3 个关键 leaf」算 low）；token 数是数量级正确的估算，截断点可能偏早或偏晚 | 出现误判伤害真实决策、或接入真 tokenizer 的成本可接受时 |

| 9 | note 在节点删除-重建周期内保留（`retiredNotes` 墓碑，会话级；fullScan 清除） | 一个被删文件的 note 会在同文件回来时复活——即使它被删是为了「清掉这条备注」；跨会话不持久（本来就不持久） | 出现「删除文件后 note 不该复活」的真实诉求时 |
| 10 | relay 是「同根共享」表面：`/internal/broadcast` 用 root 派生的确定性 token 鉴权（同一仓库所有会话共享），且 `node_update` 转发时剥离 note、剥离开启状态外的 done 评审 | 知道被监视根路径的本地进程仍可伪造中继事件；跨会话不再转发「已完成评审」环（checking 脉冲仍转发） | 出现需要更强 relay 信任模型（如进程级握手）时 |
| 11 | `/api/*` 与 WS 用**随机启动 token**（进 dashboard URL）鉴权，`/api/info` 与静态资源免鉴权；只读模式下 relay 整体关停 | token 在 URL 里（浏览器历史/日志可能泄露）；同机进程读到 dashboard URL 即获权；headless 副实例的 `get_dashboard_info` 只能给出**本实例**的链接（主实例随机 token 不可共享，无法代开主实例 URL） | 需要真正的进程级凭据（非 URL 承载）时 |
| 12 | 聚类排列模式求解不读位置存档（确定性优先，ADR 0004/D2 搁置）；Louvain resolution 固定 1.0；出生抖动为 fnv1a(path) 的有意简化 | 聚类模式下手摆拖拽位只活到下一次重渲（切回区域模式即复活、跨模式手摆位不保证保留）；碎团/大团平衡不可调，超大 monorepo 可能出现病态分团；同聚类成员出生半径 ∝ √n 无碰撞保证（靠全场 separateAllBalls 硬保证兜底，2026-09-01 D3） | 用户真实提出「聚类模式要钉住某球」时（做 per-node pin 通道）；或图大到 Louvain 分团失真时 |
| 13 | 聚类保种的代价（ADR 0004 同日修正二）：逐簇独立 eles 求解——跨簇边**不参与定位**（只画线不拽球），团间亲疏完全由 Louvain + 领地规划决定；每次重渲 = k 次 fcose 全跑 | 力布局本可表达的「跨簇吸引」被有意裁掉（贴对了社区则无感，社区划分错了没有弹簧兜底）；超大图渲染开销 ×社区数（headless 实测百级毫秒级；applyDelta 每次走全量重解） | 出现「两团明显相关却离得远」的真实视觉诉求时（考虑簇间引力通道或把弱桥并进求解）；或重解耗时肉眼可感时 |
